{
  "buildCommand": "ROOT=$(git rev-parse --show-toplevel 2>/dev/null || pwd) && npm --prefix \"$ROOT/web\" install && npm run build --prefix \"$ROOT/web\"",
  "outputDirectory": "web/dist",
  "regions": ["sin1"],
  "framework": null,
  "functions": {
    "api/*.js": {
      "runtime": "nodejs20.x",
      "memory": 1024,
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
