{
  "version": 2,
  "builds": [
    { "src": "api/ai-insight.ts", "use": "@vercel/node" },
    { "src": "api/save-result.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
